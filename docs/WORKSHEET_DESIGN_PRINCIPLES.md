# 워크시트 설계 원칙

**작성일**: 2026-01-14  
**적용 범위**: 모든 워크시트 화면 (FMEA, CP, PFD 등)

## 핵심 설계 원칙

### 1. 메뉴바 크기 및 고정

#### 메뉴바 크기
- **기준**: 브라우저 크기에 맞춰 개발
- **기본 크기**: 1440px (표준 화면 기준)
- **반응형**: viewport 크기에 따라 조정 가능
- **원칙**: 메뉴바는 브라우저 크기에 맞춰서 개발한다 (1440px 기준)

#### 메뉴바 고정
- **위치**: 항상 고정 (position: fixed 또는 sticky)
- **스크롤 영향 없음**: 가로/세로 스크롤 시에도 움직이지 않음
- **z-index**: 다른 요소 위에 항상 표시
- **원칙**: 메뉴바는 항상 고정되어 가로세로 스크롤에도 움직이지 않는다

**구현 방법**:
```css
/* 메뉴바 컨테이너 */
.menu-bar {
  position: fixed;
  top: 0;
  left: 53px; /* 사이드바 너비 + 간격 */
  width: 1440px; /* 기본 크기 */
  z-index: 99;
}
```

### 2. 스크롤바 정책

#### 좌우 스크롤바 (가로 스크롤)
- **원칙**: 컨테이너에 **1개만** 사용
- **위치**: 메인 워크시트 컨테이너
- **중첩 스크롤바 금지**: 내부 테이블이나 다른 요소에 추가 스크롤바 생성 금지
- **핵심 원칙**: 좌우 스크롤바는 컨테이너에 1개만 사용한다

**구현 방법**:
```css
/* 메인 워크시트 컨테이너 */
.worksheet-container {
  overflow-x: auto; /* 가로 스크롤 1개만 */
  overflow-y: hidden; /* 세로 스크롤은 워크시트에만 */
}
```

#### 세로 스크롤바 (세로 스크롤)
- **원칙**: 워크시트 영역만 조정
- **위치**: 워크시트 테이블 컨테이너
- **메뉴바/헤더 제외**: 메뉴바와 헤더는 스크롤에 포함되지 않음
- **핵심 원칙**: 세로 스크롤바는 워크시트만 조정하게 만든다

**구현 방법**:
```css
/* 워크시트 테이블 컨테이너 */
.worksheet-table-container {
  overflow-x: hidden; /* 가로 스크롤은 메인 컨테이너에서 */
  overflow-y: auto; /* 세로 스크롤만 여기서 */
}
```

### 3. 레이아웃 구조

```
┌─────────────────────────────────────┐
│ 사이드바 (고정, 48px)                │
├─────────────────────────────────────┤
│ 메뉴바 (고정, 1440px, 스크롤 없음)   │
├─────────────────────────────────────┤
│ 헤더 영역 (고정, 스크롤 없음)        │
├─────────────────────────────────────┤
│ 워크시트 컨테이너 (가로 스크롤 1개) │
│ ┌─────────────────────────────────┐ │
│ │ 워크시트 테이블 (세로 스크롤)    │ │
│ │                                 │ │
│ │ [세로 스크롤바]                 │ │
│ └─────────────────────────────────┘ │
│ [가로 스크롤바]                      │
└─────────────────────────────────────┘
```

## 구현 가이드

### 메뉴바 구현 예시

```tsx
// 메뉴바 컴포넌트
<div className="fixed top-0 left-[53px] w-[1440px] z-[99]">
  {/* 메뉴바 내용 */}
</div>

// 메인 콘텐츠 영역 (메뉴바 아래)
<div className="mt-[메뉴바높이]">
  {/* 콘텐츠 */}
</div>
```

### 스크롤 컨테이너 구조

```tsx
// 메인 워크시트 컨테이너 (가로 스크롤 1개만)
<div className="overflow-x-auto overflow-y-hidden">
  {/* 워크시트 테이블 컨테이너 (세로 스크롤만) */}
  <div className="overflow-x-hidden overflow-y-auto">
    <table>
      {/* 워크시트 테이블 */}
    </table>
  </div>
</div>
```

## 검증 체크리스트

새 워크시트 개발 시 다음을 확인:

- [ ] 메뉴바는 1440px 기준으로 개발되었는가?
- [ ] 메뉴바는 스크롤 시에도 고정되어 있는가?
- [ ] 가로 스크롤바는 컨테이너에 1개만 있는가?
- [ ] 세로 스크롤바는 워크시트 영역에만 있는가?
- [ ] 메뉴바와 헤더는 스크롤에 포함되지 않는가?

## 4. 셀 텍스트 자동 축소 (`adaptiveText`)

**위치**: `src/app/pfmea/worksheet/tabs/StructureTabCells.tsx`

### 원칙
- 텍스트가 짧으면 11px 한 줄, 길어지면 **글씨 축소 + 줄바꿈**
- **잘림(clamp/ellipsis) 금지** — 모든 텍스트가 항상 보여야 함
- 컬럼 폭에 따라 임계값을 다르게 설정

### 단계별 동작

| 단계 | 조건 | 폰트 | 줄바꿈 |
|------|------|------|--------|
| 1 | ≤ threshold | 11px | 없음 (1줄) |
| 2 | ≤ threshold × 2 | 11px | break-keep (~2줄) |
| 3 | ≤ threshold × 3 | 9px | break-keep (~3줄) |
| 4 | > threshold × 3 | 8px | break-keep (전부 표시) |

### 컬럼별 임계값 (1440px 뷰포트 기준)

| 컬럼 | 폭 설정 | 실제 px | 11px 한글 기준 | threshold |
|------|---------|---------|---------------|-----------|
| L2 (공정) | 22% | ~264px | ~23자 | **23** |
| L3 (작업요소) | auto | ~617px | ~55자 | **55** |

> 계산식: `(컬럼 px - 패딩 8px) ÷ 한글 11px ≈ threshold`

### 사용법
```tsx
import { adaptiveText } from './StructureTabCells';

// L2 셀 (좁음)
<span className={adaptiveText(text, 23)}>{text}</span>

// L3 셀 (넓음)
<span className={adaptiveText(text, 55)}>{text}</span>
```

---

## 예외 사항

### 특수 케이스
- **CP Import 페이지**: 메뉴바 1414px 고정 (특수 레이아웃)
  - 태그: `codefreeze-20260114-cp-import-layout`
  - 참고: `docs/CODEFREEZE_CP_IMPORT_LAYOUT_20260114.md`

## 관련 문서
- `docs/CODEFREEZE_FILES.md`: 코드프리즈 파일 목록
- `docs/COLOR_CHANGE_GUIDE.md`: 색상 시스템 가이드
- `docs/ZEBRA_STRIPE_RULES.md`: 줄무늬 규칙

