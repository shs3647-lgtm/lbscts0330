# 2026-01-25 모달 UI 통일 및 버그 수정 작업

> **작업일**: 2026-01-25  
> **작업자**: AI Assistant  
> **커밋**: `0acd3ae7`  

---

## 📋 작업 개요

PFMEA 워크시트의 모든 입력 모달 UI를 통일하고, 구조분석에서 발견된 버그를 수정했습니다.

---

## ✅ 완료된 작업

### 1. 모달 스타일 통일 (5개 모달)

| 모달 | 파일 | 변경 내용 |
|------|------|----------|
| ProcessSelectModal | `src/app/pfmea/worksheet/ProcessSelectModal.tsx` | 스타일 통일 |
| WorkElementSelectModal | `src/app/pfmea/worksheet/WorkElementSelectModal.tsx` | 스타일 통일 |
| DataSelectModal | `src/components/modals/DataSelectModal.tsx` | 스타일 통일 |
| BaseSelectModal | `src/components/modals/BaseSelectModal.tsx` | 스타일 통일 |
| SpecialCharSelectModal | `src/components/modals/SpecialCharSelectModal.tsx` | 스타일 통일 |

#### 통일된 스타일 상세

| 속성 | 변경 전 | 변경 후 |
|------|--------|--------|
| 위치 (top) | 80~200px (제각각) | **130px** (통일) |
| 헤더 색상 | 파란색/빨간색 (제각각) | **주황색** (`from-orange-500 to-orange-600`) |
| 닫기 버튼 | 반투명 흰색 | **빨간색** (`bg-red-500`) |
| 라벨 | 없음 | **"선택입력 :"** (노란색 11px) |
| modalHeight | 200px | **400px** |

#### 코드 예시

```tsx
// 헤더 스타일 (통일)
<div className="flex items-center justify-between px-3 py-2 bg-gradient-to-r from-orange-500 to-orange-600 text-white cursor-move select-none">
  <div className="flex items-center gap-2">
    <span className="text-yellow-300 text-[11px] font-bold">선택입력 :</span>
    <span>🔧</span>
    <h2 className="text-[11px] font-bold">작업요소 선택</h2>
  </div>
  <button className="text-[10px] px-2 py-0.5 bg-red-500 hover:bg-red-600 text-white rounded font-bold">
    닫기
  </button>
</div>
```

---

### 2. 트리뷰 헤더 개선

| 파일 | 변경 내용 |
|------|----------|
| `src/app/pfmea/worksheet/panels/TreePanel/BaseTreePanel.tsx` | "트리뷰:" 라벨 추가 |

#### 변경 내용

- 트리뷰 헤더에 **"트리뷰:"** 라벨 추가 (노란색 11px 굵은 글씨)
- 모달과 트리뷰 구분 명확화

```tsx
<span className="text-yellow-300 text-[11px] font-bold mr-1">트리뷰:</span>
{config.icon} {config.title}
```

---

### 3. 작업요소 삭제 버그 수정

| 문제 | 원인 | 수정 내용 |
|------|------|----------|
| 전체 삭제 시 테이블 구조 깨짐 | 빈 배열 저장 시 rows 계산 오류 | 플레이스홀더 `(작업요소 없음-삭제)` 추가 |
| 삭제 시 기본 데이터까지 저장됨 | `elements` 전체에서 필터링 | `existingL3` 기반 필터링 |
| 모달에 플레이스홀더 표시됨 | 필터링 조건 미흡 | "없음"/"삭제" 포함 항목 제외 |

#### 수정 파일

| 파일 | 변경 내용 |
|------|----------|
| `StructureTab.tsx` | 빈 배열 저장 시 플레이스홀더 추가 |
| `WorkElementSelectModal.tsx` | 삭제 로직 수정, 플레이스홀더 필터링 |

---

### 4. PFMEA/DFMEA 데이터 분리

| 문제 | 원인 | 수정 내용 |
|------|------|----------|
| PFMEA 모달에 DFMEA 데이터 표시 | `DEFAULT_ITEMS`가 DFMEA 전용 | URL 경로 기반 분리 |

#### 수정 코드

```tsx
// DataSelectModal.tsx
const isPFMEA = typeof window !== 'undefined' && window.location.pathname.includes('/pfmea');
const defaultItemsSource = isPFMEA ? PFMEA_DEFAULT_ITEMS : DEFAULT_ITEMS;

if (defaultItemsSource[itemCode]) {
  allItems = [...defaultItemsSource[itemCode]];
}
```

---

### 5. 누락 셀 스타일

| 파일 | 변경 내용 |
|------|----------|
| `StructureTabCells.tsx` | 누락 셀 주황색 배경 (#fed7aa) |

#### 변경 내용

- 플레이스홀더 조건에 "없음", "삭제" 추가
- 스트라이프 패턴 → 단색 주황색 배경

```tsx
const isPlaceholder = value.includes('추가') || value.includes('클릭') || 
  value.includes('필요') || value.includes('없음') || value.includes('삭제');

const bgStyle: React.CSSProperties = isPlaceholder
  ? { background: '#fed7aa' }  // 주황색 배경 (orange-200)
  : { background: zebraBg };
```

---

## 📋 미해결 이슈 (별도 작업 필요)

| 이슈 | 설명 | 예상 작업량 |
|------|------|-----------|
| **기초정보 FMEA ID별 연동** | 엑셀 Import 데이터가 `pfmea_master_data`에 전역 저장됨. FMEA ID별 분리 필요 | 중 |
| **기초정보 → 워크시트 연동** | 기초정보 저장 시 DB만 사용, 워크시트는 localStorage 사용. 동기화 필요 | 중 |

### 해결 방안 (제안)

1. **localStorage 키 변경**: `pfmea_master_data` → `pfmea_master_data_${fmeaId}`
2. **모달에서 FMEA ID 전달**: props로 fmeaId 받아서 해당 데이터만 로드
3. **기초정보 저장 시 동기화**: DB 저장 + localStorage 동기화

---

## 🔗 관련 커밋

| 커밋 | 메시지 |
|------|--------|
| `560e3880` | feat: 트리뷰 헤더에 '트리뷰:' 라벨 추가 (노란색 11px 굵은 글씨) |
| `fc544549` | fix: DataSelectModal PFMEA/DFMEA 데이터 분리 - URL 경로 기반 |
| `44158c5f` | feat: 모든 모달 스타일 통일 (위치 130px, 주황색 헤더, 빨간 닫기, 선택입력 라벨) |
| `0acd3ae7` | docs: QA 테스트 결과 업데이트 |

---

## 📁 수정된 파일 목록

```
src/app/pfmea/worksheet/
├── ProcessSelectModal.tsx          # 위치/색상/라벨 변경
├── WorkElementSelectModal.tsx      # 위치/색상/라벨/삭제로직 수정
└── tabs/
    ├── StructureTab.tsx            # 빈 배열 저장 시 플레이스홀더 추가
    └── StructureTabCells.tsx       # 누락 셀 주황색 배경

src/app/pfmea/worksheet/panels/TreePanel/
└── BaseTreePanel.tsx               # "트리뷰:" 라벨 추가

src/components/modals/
├── DataSelectModal.tsx             # 위치/색상/라벨/PFMEA분리
├── BaseSelectModal.tsx             # 위치/색상/라벨 변경
└── SpecialCharSelectModal.tsx      # 위치/색상/라벨 변경

src/components/modals/data/
└── defaultItems.ts                 # PFMEA_DEFAULT_ITEMS export 추가
```

---

## 🧪 테스트 체크리스트

- [x] 공정선택 모달 - 주황색 헤더, 빨간 닫기
- [x] 작업요소 모달 - 주황색 헤더, 빨간 닫기
- [x] 기능분석 모달 - 주황색 헤더, 빨간 닫기
- [x] 고장분석 모달 - 주황색 헤더, 빨간 닫기
- [x] 특별특성 모달 - 주황색 헤더, 빨간 닫기
- [x] 트리뷰 헤더 - "트리뷰:" 라벨 표시
- [x] 작업요소 전체 삭제 - 구조 깨짐 없음
- [x] 작업요소 반복 삭제 - 안정적 동작
- [x] PFMEA 기능분석 - DFMEA 데이터 없음
- [x] 누락 셀 - 주황색 배경 표시
