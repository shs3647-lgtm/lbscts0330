/**
 * @file layout.tsx
 * @description PM (Process Management) 모듈 레이아웃
 * @version 1.0.0
 */

import React from 'react';

export default function PmLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
