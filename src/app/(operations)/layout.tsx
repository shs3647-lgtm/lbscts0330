/**
 * @file Operations 가족 레이아웃
 * @description WS, PM 모듈 전용 레이아웃
 * — "한지붕 세가족" 아키텍처의 가족3
 */

export default function OperationsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
