/**
 * @file FMEA Core 가족 레이아웃
 * @description PFMEA, DFMEA, CP, PFD 모듈 전용 레이아웃
 * — "한지붕 세가족" 아키텍처의 가족1
 */

export default function FmeaCoreLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
